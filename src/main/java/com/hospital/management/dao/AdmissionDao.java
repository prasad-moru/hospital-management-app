package com.hospital.management.dao;import com.hospital.management.model.*;import java.sql.*;import java.time.*;import java.util.*;public interface AdmissionDao{Optional<Admission>findById(Long id)throws SQLException;Optional<Admission>findActiveByPatientId(Long id)throws SQLException;Page<Admission>findAll(PageRequest p,Long patientId,Long doctorId,Long departmentId,Long roomId,LocalDate from,LocalDate to,String status)throws SQLException;long create(Connection c,Admission a)throws SQLException;boolean updateActiveAdmission(Connection c,Admission a)throws SQLException;boolean transferBed(Connection c,Long id,Long target,String status)throws SQLException;void recordTransfer(Connection c,Long id,Long from,Long to,String reason,Long user)throws SQLException;boolean discharge(Connection c,Long id,LocalDateTime date,String summary,String status)throws SQLException;boolean cancel(Connection c,Long id,String status)throws SQLException;boolean existsActiveAdmissionForPatient(Long id)throws SQLException;boolean existsActiveAdmissionForBed(Long id)throws SQLException;Optional<Admission>lockById(Connection c,Long id)throws SQLException;}
