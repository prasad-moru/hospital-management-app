package com.hospital.management.dao;import com.hospital.management.model.*;import java.sql.*;import java.time.LocalDate;import java.util.Optional;public interface PrescriptionDao {Optional<Prescription>findById(Long id)throws SQLException;Optional<Prescription>findByMedicalRecordId(Long id)throws SQLException;Page<Prescription>findAll(PageRequest p,Long patientId,Long doctorId,Long departmentId,LocalDate from,LocalDate to,String status)throws SQLException;long count(String search,Long patientId,Long doctorId,Long departmentId,LocalDate from,LocalDate to,String status)throws SQLException;long createPrescription(Connection c,Prescription p)throws SQLException;boolean updatePrescription(Connection c,Prescription p)throws SQLException;boolean updateStatus(Connection c,Long id,String status)throws SQLException;boolean existsByMedicalRecordId(Long id)throws SQLException;boolean existsByMedicalRecordIdExcludingPrescription(Long mr,Long rx)throws SQLException;}
