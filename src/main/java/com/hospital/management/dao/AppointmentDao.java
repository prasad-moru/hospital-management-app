package com.hospital.management.dao;
import com.hospital.management.model.*;import java.sql.*;import java.time.*;import java.util.*;
public interface AppointmentDao {Optional<Appointment> findById(Long id)throws SQLException;Page<Appointment> findAll(PageRequest p,Long patientId,Long doctorId,Long departmentId,LocalDate from,LocalDate to,String status)throws SQLException;long count(String search,Long patientId,Long doctorId,Long departmentId,LocalDate from,LocalDate to,String status)throws SQLException;long create(Connection c,Appointment a)throws SQLException;boolean updateDetails(Connection c,Appointment a)throws SQLException;boolean reschedule(Connection c,Long id,LocalDate date,LocalTime start,LocalTime end,String reason,String notes)throws SQLException;boolean updateStatus(Connection c,Long id,String status)throws SQLException;boolean existsDoctorBooking(Long doctorId,LocalDate date,LocalTime start,Long excludeId)throws SQLException;boolean existsPatientOverlap(Long patientId,LocalDate date,LocalTime start,LocalTime end,Long excludeId)throws SQLException;List<LocalTime> findBookedStartTimes(Long doctorId,LocalDate date)throws SQLException;}
