package com.hospital.management.dao;import com.hospital.management.model.*;import java.math.*;import java.sql.*;import java.time.*;import java.util.*;public interface BillDao{Optional<Bill>findById(Long id)throws SQLException;Optional<Bill>findByIdForUpdate(Connection c,Long id)throws SQLException;Optional<Bill>findByBillNumber(String n)throws SQLException;Optional<Bill>findByAppointmentId(Long id)throws SQLException;Page<Bill>findAll(PageRequest p,Long patientId,Long doctorId,Long departmentId,LocalDate from,LocalDate to,String status)throws SQLException;long count(String q,Long patientId,Long doctorId,Long departmentId,LocalDate from,LocalDate to,String status)throws SQLException;long createBill(Connection c,Bill b)throws SQLException;boolean updateBill(Connection c,Bill b)throws SQLException;boolean updateFinancialSummary(Connection c,Long id,BigDecimal sub,BigDecimal tax,BigDecimal discount,BigDecimal total,BigDecimal paid,BigDecimal balance,String status)throws SQLException;boolean updateStatus(Connection c,Long id,String status)throws SQLException;boolean existsActiveBillByAppointment(Long id)throws SQLException;boolean existsActiveBillByAppointmentExcludingBill(Long appointmentId,Long billId)throws SQLException;}
